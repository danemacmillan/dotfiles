#!/usr/bin/env bash
source "${DOTFILES_BOOTSTRAP_FILE}"

##
# Ensure correct, non-MacOS version of curl is installed.
#
# MacOS carries an old version, so replace it with the one defined in brew.
#
# @author Dane MacMillan <work@danemacmillan.com>
# @link https://github.com/danemacmillan/dotfiles
# @license MIT
#

brew install curl
